import { useEffect,useState } from "react"; import {Link} from "react-router-dom"; import {getBills,cancelBill} from "../../services/billingService"; import "../../styles/Billing.css";
export default function Bills(){const [data,setData]=useState([]);const [search,setSearch]=useState("");const [loading,setLoading]=useState(true);const load=()=>{setLoading(true);getBills({search}).then(r=>setData(r.data?.data||[])).catch(()=>setData([])).finally(()=>setLoading(false));};useEffect(load,[]);return <div className="billing-page"><div className="billing-header"><div><h1>Bills</h1><p>View, search and manage billing transactions.</p></div><Link className="billing-primary-link" to="/billing/entry">+ New Bill</Link></div><div className="billing-card"><div className="billing-toolbar"><input placeholder="Search bill or customer" value={search} onChange={e=>setSearch(e.target.value)}/><button className="billing-secondary" onClick={load}>Search</button></div><div className="billing-table-wrap"><table className="billing-table"><thead><tr><th>Bill No</th><th>Store</th><th>Customer</th><th>Payment</th><th>Amount</th><th>Created By</th><th>Status</th><th></th></tr></thead><tbody>{loading?<tr><td colSpan="8">Loading...</td></tr>:data.map(b=><tr key={b.id}><td><Link to={`/billing/bills/${b.id}`}>{b.bill_no}</Link></td><td>{b.store_name||"-"}</td><td>{b.customer_name||"-"}</td><td>{b.payment_type||"-"}</td><td>₹{Number(b.grand_total||0).toFixed(2)}</td><td>{b.created_by_name||"-"}</td><td><span className={`billing-status ${String(b.status).toLowerCase()}`}>{b.status}</span></td><td>{b.status!=="CANCELLED"&&<button className="billing-danger" onClick={async()=>{if(window.confirm("Cancel this bill?")){await cancelBill(b.id);load();}}}>Cancel</button>}</td></tr>)}</tbody></table></div></div></div>}
